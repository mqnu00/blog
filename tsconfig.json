{
  "compilerOptions": {
    /* 基础设置 */
    "target": "ESNext",                // 输出的 JS 版本
    "module": "NodeNext",                // 使用 ES 模块
    "moduleResolution": "nodenext",        // Node 风格的模块解析
    "strict": true,                    // 开启严格模式
    "jsx": "preserve",                 // 保留 JSX（如果你用 Vue/React）

    /* 路径和类型 */
    "baseUrl": ".",                    // 项目根目录为基准
    "types": ["vitepress", "node", "vue"],    // 引入 VitePress 和 Node 类型声明

    /* 输出控制 */
    "esModuleInterop": true,           // 允许默认导入 CommonJS 模块
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,         // 支持导入 JSON 文件
    "isolatedModules": true,           // 确保每个文件独立编译
    "skipLibCheck": true               // 跳过库的类型检查，加快编译
  },
  "include": [
    "./blog/.vitepress/**/*",            // VitePress 配置文件
    "**/*.ts",                    // 文档里可能的 TS 文件
    "./blog/.vitepress/**/*.vue",
    "**/*.d.ts"                   // 类型声明文件
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
